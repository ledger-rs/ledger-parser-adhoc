use crate::{xact::Xact, account::Account, amount::Amount};

/**
 * posts.h + posts.cc
 */

pub struct Post {
    // xact: &Xact,
    // account: &Account,

    amount: Amount,
    // amount_expr
    // cost
    // given_cost
    // assigned_amount

    // timestamps:
    // checkin
    // checkout

    // payee: Option<String>
}